from ast import If
import numbers
from tokenize import Number


def suma(a,b: int) -> None:
    """Permite sumar 2 numeros

    Args:
        a (int): El primer numero ingresado
        b (int): El segundo numero ingresado

    Returns:
        El resultado de la suma de los 2 numeros

        """
    # En consola ejecuta lo siguiente
    # py -m doctest pruebas_doctring.py -v
    return a + b
    

